export const metadata = {
  title: 'NFI Report — Studio CMS',
  description: 'Sanity Studio pour gérer le contenu de NFI Report',
};

export default function StudioLayout({ children }: { children: React.ReactNode }) {
  return children;
}
